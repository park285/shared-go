{
  "contract_version": 1,
  "service": "chatbotgo",
  "role": "bot",
  "profile_id": "chatbotgo-contract-fixture",
  "workers": {
    "webhook_inbox": {
      "executor": {"enabled": true, "configured_workers": 16, "attempt_timeout": {"mode": "fixed", "milliseconds": 120000}},
      "queue": {"capacity": {"mode": "unbounded", "items": null}, "max_age": {"mode": "fixed", "milliseconds": 600000}},
      "settings": {"prefetch_capacity": 128, "cleanup_timeout_ms": 15000, "poll_interval_ms": 500, "claim_batch_size": 16, "processing_lease_ms": 1440000, "max_attempts": 6}
    },
    "command": {
      "executor": {"enabled": true, "configured_workers": 10, "attempt_timeout": {"mode": "per_job", "milliseconds": null}},
      "queue": {"capacity": {"mode": "bounded", "items": 100}, "max_age": {"mode": "none", "milliseconds": null}},
      "settings": {"shutdown_timeout_ms": 30000}
    },
    "draw": {
      "executor": {"enabled": true, "configured_workers": 2, "attempt_timeout": {"mode": "fixed", "milliseconds": 120000}},
      "queue": {"capacity": {"mode": "bounded", "items": 16}, "max_age": {"mode": "fixed", "milliseconds": 300000}},
      "settings": {"shutdown_timeout_ms": 30000}
    },
    "summary": {
      "executor": {"enabled": true, "configured_workers": 2, "attempt_timeout": {"mode": "fixed", "milliseconds": 120000}},
      "queue": {"capacity": {"mode": "bounded", "items": 16}, "max_age": {"mode": "fixed", "milliseconds": 300000}},
      "settings": {"shutdown_timeout_ms": 30000}
    },
    "compaction": {
      "executor": {"enabled": true, "configured_workers": 1, "attempt_timeout": {"mode": "fixed", "milliseconds": 30000}},
      "queue": {"capacity": {"mode": "bounded", "items": 8}, "max_age": {"mode": "fixed", "milliseconds": 300000}},
      "settings": {"shutdown_timeout_ms": 15000}
    }
  }
}
